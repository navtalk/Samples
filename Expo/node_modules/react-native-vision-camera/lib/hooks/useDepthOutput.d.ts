import type { FrameDroppedReason } from '../specs/common-types/FrameDroppedReason';
import type { Depth } from '../specs/instances/Depth.nitro';
import type { CameraDepthFrameOutput, DepthFrameOutputOptions } from '../specs/outputs/CameraDepthFrameOutput.nitro';
type BaseDepthOptions = Pick<DepthFrameOutputOptions, 'targetResolution' | 'allowDeferredStart' | 'dropFramesWhileBusy' | 'enableFiltering'>;
interface Props extends Partial<BaseDepthOptions> {
    /**
     * @worklet
     */
    onDepth?: (depth: Depth) => void;
    onDepthFrameDropped?: (reason: FrameDroppedReason) => void;
}
export declare function useDepthOutput({ targetResolution, enableFiltering, onDepth, onDepthFrameDropped, dropFramesWhileBusy, allowDeferredStart, }: Props): CameraDepthFrameOutput;
export {};
