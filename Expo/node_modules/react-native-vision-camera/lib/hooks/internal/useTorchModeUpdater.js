import { useEffect } from 'react';
export function useTorchModeUpdater(controller, torchMode) {
    useEffect(() => {
        if (controller == null)
            return;
        if (torchMode == null)
            return;
        controller.setTorchMode(torchMode);
    }, [controller, torchMode]);
}
